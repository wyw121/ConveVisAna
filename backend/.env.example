# 后端环境变量配置示例
# 复制此文件为 .env 并填入实际值

# ============ LLM API 配置 ============
# 使用硅基流动免费 API（推荐）
CHATAIAPI_KEY=sk-your-siliconflow-api-key-here

# 或使用其他 OpenAI 兼容 API
# OPENAI_API_KEY=your_openai_api_key_here

# 必须：设置为硅基流动 API 地址
CHATAI_BASE_URL=https://api.siliconflow.cn/v1

# 可选：超时配置（支持 "connect,read" 或单值）
# 例如：连接 20s、读取 60s
# CHATAI_TIMEOUT=20,60

# 可选：重试配置（对 429/5xx 指数退避重试）
# CHATAI_RETRY_TOTAL=3
# CHATAI_RETRY_BACKOFF=1.5

# ============ 模型默认配置 ============
# ✅ 推荐首选：Qwen/Qwen2.5-7B-Instruct
# 测试结果：
#   - 成功率：100% (3/3 测试全通过)
#   - 平均响应：1.88秒
#   - JSON 格式：完美兼容
#   - 费用：完全免费
#
# 其他可用免费模型：
#   - Qwen/Qwen2-7B-Instruct (旧版本，速度更快 1.01秒)
#   - deepseek-ai/DeepSeek-R1-Distill-Qwen-7B (推理强但JSON兼容性差)

# 质量评估默认模型（推荐：Qwen/Qwen2.5-7B-Instruct）
DEFAULT_MODEL_EVALUATION=Qwen/Qwen2.5-7B-Instruct

# 流程分析默认模型（推荐：Qwen/Qwen2.5-7B-Instruct）
DEFAULT_MODEL_FLOW_ANALYSIS=Qwen/Qwen2.5-7B-Instruct

# 通用默认模型（推荐：Qwen/Qwen2.5-7B-Instruct）
DEFAULT_MODEL_GENERAL=Qwen/Qwen2.5-7B-Instruct

# ============ DeepEval 配置 ============
# 禁用遥测数据收集
DEEPEVAL_TELEMETRY_OPT_OUT=YES

# ============ CORS 配置 ============
# 允许的前端域名（逗号分隔）
ALLOWED_ORIGINS=http://localhost:3000,https://convelyze.pages.dev

# ============ 分析限制 ============
# 最大评估对话数
MAX_CONVERSATIONS_TO_EVALUATE=10

# 最大分析轮次
MAX_TURNS_TO_ANALYZE=50

# ============ 可选配置 ============
# 环境标识
ENV=development

# 日志级别
LOG_LEVEL=INFO
